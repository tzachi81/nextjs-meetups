// /api/new-meetup

export const handler = (request: Request, response: Response) => {

  if(request.method === 'POST'){
    const data = request.body; // new meetup form details sent

    // const {title, image, address, description} = data;
  }
}